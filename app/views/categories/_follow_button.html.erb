<%= link_to toggle_following_category_path(tag), remote: true, method: :post, data: {action: "ajax:success->follow-tag#toggle", disable_with: disable_with_spinner(tag.followed_by?(current_user) ? t("unfollow") : t("follow"))}, class: tag.followed_by?(current_user) ? "btn-cancel" : 'btn-primary' do %>
  <%= tag.followed_by?(current_user) ? t("unfollow") : t("follow") %>
<% end %>